const express = require("express");
const { validationResult } = require("express-validator");

// const validateNotificationStoreData = [
//   body("title").isEmpty().withMessage("Title is empty"),
//   // Add more validation rules for other fields...
// ];
