# Test Environment Configuration
DB_USERNAME=root
DB_PASSWORD=Quadque#2024
DB_NAME=email_marketing_test
DB_HOST=127.0.0.1
NODE_ENV=test

# OAuth and App URLs (for testing)
REDIRECT_URI=http://localhost:5000/google/callback
BASE_URL=http://localhost:5000
FRONTEND_AUTH_URL=http://localhost:3000/auth
GMAIL_API_PROFILE=https://gmail.googleapis.com/gmail/v1/users

# Session and Security (for testing)
SESSION_SECRET=test-session-secret-key

# CORS Configuration (for testing)
CORS_WHITELIST=http://localhost:3000,http://localhost:5000